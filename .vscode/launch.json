{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Local Debug",
            "type": "go",
            "request": "launch",
            "env": {
            },
            "mode": "debug",
            "program": "${workspaceRoot}"
        }
    ]
}